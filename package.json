{
  "name": "homedispatcher",
  "version": "0.1.0",
  "private": true,
  "description": "ДомДиспетчер — building/dispatch management platform (React frontend + NestJS backend)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "docker compose up -d && node scripts/wait-for-infra.mjs && npm run db:migrate && npm run dev",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\"",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "infra:logs": "docker compose logs -f",
    "db:migrate": "npm run prisma:migrate -w backend",
    "db:deploy": "npm run prisma:deploy -w backend",
    "db:generate": "npm run prisma:generate -w backend",
    "db:seed": "npm run prisma:seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=20"
  }
}
